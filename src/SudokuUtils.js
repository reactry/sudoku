

function sudokuIsCorrect (sudoku) {
	//
}

function sudokuHasUniqueSolution (sudoku) {
	//
}

function getFilledSudoku () {
	let sudoku = new Array[9 * 9];
	return sudoku;
}

function getSudoku () {
	let sudoku = new Array[9 * 9];
	let solution = new Array[9 * 9];
	return [sudoku, solution];
}

module.exports = {
	getSudoku: getSudoku
};
